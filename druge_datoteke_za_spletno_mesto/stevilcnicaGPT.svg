<svg width="3800" height="3800" xmlns="http://www.w3.org/2000/svg">
  <!-- Background circle -->
  <circle cx="1900" cy="1900" r="1900" fill="none" stroke="black" stroke-width="10"/>

  <!-- Tick marks -->
  <g transform="translate(1900, 1900)">
    <!-- Draw tick marks with labels -->
    {Array.from({ length: 366 }).map((_, i) => {
      const angle = (360 / 365.25) * i;
      const radians = (angle - 90) * (Math.PI / 180);
      const tickStartX = 1800 * Math.cos(radians);
      const tickStartY = 1800 * Math.sin(radians);
      const tickEndX = (1800 - 50) * Math.cos(radians);
      const tickEndY = (1800 - 50) * Math.sin(radians);
      const textX = (1800 - 70) * Math.cos(radians);
      const textY = (1800 - 70) * Math.sin(radians);

      return (
        <g key={i}>
          <line x1={tickStartX} y1={tickStartY} x2={tickEndX} y2={tickEndY} stroke="black" stroke-width="4"/>
          <text x={textX} y={textY} fill="black" font-size="36" text-anchor="middle" dy="10">
            {i + 1}
          </text>
        </g>
      );
    })}
  </g>
</svg>

